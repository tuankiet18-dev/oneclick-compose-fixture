services:
  frontend:
    build:
      context: ./frontend
    depends_on:
      api:
        condition: service_started
    expose:
      - "3000"

  api:
    build:
      context: ./api
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://oneclick:oneclick@db:5432/oneclick_fixture}
    depends_on:
      db:
        condition: service_healthy
    expose:
      - "8000"

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: oneclick_fixture
      POSTGRES_USER: oneclick
      POSTGRES_PASSWORD: oneclick
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U oneclick -d oneclick_fixture"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
